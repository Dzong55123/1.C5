def sum2(n):
    if n == 1:
        return 2
    else:
        return sum2(n-1) + 2 * n

n = int(input("請輸入n（n>=1）："))

if n >= 1:
    result = sum2(n)
    print(f"sum2({n}) = {result}")
else:
    print("請輸入大於等於1的正整數")
